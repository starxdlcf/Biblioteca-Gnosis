import { BaseController } from "../../base/base.controller.js";

export class GeneroController extends BaseController {
  constructor(service) {
    super(service);
  }
}

export default GeneroController;
